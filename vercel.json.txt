{
  "version": 2,
  "builds": [
    {
      "src": "api/webhook.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "runtime": "python3.10"
      }
    }
  ],
  "routes": [
    {
      "src": "/webhook/(.*)",
      "dest": "api/webhook.py"
    },
    {
      "src": "/health",
      "dest": "api/webhook.py"
    }
  ],
  "env": {
    "PYTHON_VERSION": "3.10"
  }
}
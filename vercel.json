{
  "version": 2,
  "builds": [
    {
      "src": "api/webhook.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/health",
      "dest": "api/webhook.py"
    },
    {
      "src": "/webhook/([^/]+)",
      "dest": "api/webhook.py"
    }
  ]
}
